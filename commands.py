class Commands:
    def __init__(self):
        self.STOP_CAPTURE = "$0"
        self.SINGLE_CAPTURE = "$1"  # Capture single image
        self.CONTINUOUS_CAPTURE = "$2"  # Read continuously from epc901
